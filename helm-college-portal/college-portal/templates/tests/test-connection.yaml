apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "college-portal.fullname" . }}-test-connection"
  labels:
    {{- include "college-portal.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "college-portal.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
